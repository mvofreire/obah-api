import { makeStyles } from "@material-ui/styles";

export default makeStyles({
  root: {
    width: 600,
    margin: "auto",
    position: "relative",
    top: 80,
  },
  info: {
    marginBottom: 20,
  },
});
